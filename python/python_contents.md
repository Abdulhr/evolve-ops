
## **Conditionals**

* Boolean logic (`True`, `False`)
* Conditional statements: `if`, `elif`, `else`
* Comparison and logical operators (`==`, `!=`, `and`, `or`, `not`)
* Nested conditionals and operator precedence
* Truthy and falsy values in Python

---

## **Loops**

* `while` and `for` loops
* Iterating over sequences (strings, lists)
* Loop control: `break`, `continue`, `pass`
* Using `range()`
* Nested loops

---

## **Exceptions**

* Error handling using `try`, `except`, `else`, `finally`
* Raising exceptions (`raise`)
* Specific vs generic exceptions
* Using exceptions for validation and safe file operations

---

## **Libraries**

* Importing modules (`import`, `from ... import`)
* Common standard libraries: `random`, `statistics`, `sys`
* Command-line arguments (`sys.argv`)
* Installing and using third-party libraries (`pip`)
* Reading and understanding documentation

---

## **Unit Tests**

* Writing basic tests using `assert`
* Validating function output and behavior
* Concepts of test coverage and edge cases
* Organizing and automating test cases
* Debugging and refactoring with tests

---

## **File I/O**

* Opening, reading, and writing files (`open`, `with`)
* File modes: `r`, `w`, `a`, binary modes
* Working with CSV and JSON files
* File and directory handling (`os`, `csv` modules)
* Handling file-related exceptions

---

## **Regular Expressions**

* Using the `re` module
* Common regex patterns and metacharacters (`^`, `$`, `.`, `*`, `+`, `?`, `[]`)
* Matching, searching, and replacing text
* Capturing groups and validation use cases
* Regex best practices and pitfalls

---

## **Object-Oriented Programming**

* Classes and objects
* Instance variables and methods
* Special methods (`__init__`, `__str__`, `__repr__`)
* Inheritance and `super()`
* Encapsulation and when to use OOP design

---

## **Et Cetera (Advanced Concepts)**

* List, dictionary, and set comprehensions
* Lambda functions
* Decorators (brief intro)
* Type hints and static typing
* Structural pattern matching (`match` statement, Python 3.10+)
* Code organization and modularity